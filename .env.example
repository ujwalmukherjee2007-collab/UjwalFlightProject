# Only needed for production deployment. Leave unset for local development —
# Vite's dev server proxy (see vite.config.js) handles /api locally without this.
#
# Set this to your deployed backend's URL, e.g.:
# VITE_API_URL=https://flight-tracker-backend.onrender.com
VITE_API_URL=
